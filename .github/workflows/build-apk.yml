name: Build APK

on:
  push:
    branches: [master, main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system deps
        run: |
          sudo apt update
          sudo apt install -y python3-pip python3-dev build-essential git \
            autoconf libtool pkg-config zlib1g-dev libncurses5-dev \
            libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev \
            openjdk-17-jdk libltdl-dev libgl1-mesa-dev libegl1-mesa-dev \
            libgles2-mesa-dev
          pip install buildozer cython==0.29.37

      - name: Build APK
        run: |
          # Add system aidl to PATH (buildozer's own SDK lacks build-tools)
          export PATH="/usr/local/lib/android/sdk/build-tools/35.0.0:$PATH"
          which aidl
          buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: autocompose-debug-apk
          path: bin/*.apk
